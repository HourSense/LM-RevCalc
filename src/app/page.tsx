import CalculatorPage from '@/app/calculator/page';

/**
 * The main page component that renders the Calculator page.
 *
 * @returns {JSX.Element} The rendered Calculator component.
 */
const Page = () => {
    return <CalculatorPage />;
};

export default Page;
